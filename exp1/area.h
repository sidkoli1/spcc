#define AREA_SQUARE(side) ((side) * (side))
#define AREA_RECTANGLE(length, breadth) ((length) * (breadth))
#define AREA_TRIANGLE(base, height) (0.5 * (base) * (height))
#define AREA_CIRCLE(radius) (3.14159 * (radius) * (radius))
